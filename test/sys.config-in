[{ocs,
      [{radius,
               [{auth,
                      [{{127,0,0,1}, 9912, []}]},
               {acct,
                     [{{127,0,0,1}, 9913, [{rotate, 86400000}]}]}]},
      {tls_key, "%CERT_DIR%/key.pem"},
      {tls_cert, "%CERT_DIR%/cert.pem"},
      {tls_cacert, "%CERT_DIR%/CAcert.pem"}]},
{radius,
      [{sock_opts, [{recbuf, 16384}]}]},
{inets,
      [{services,
         [{httpd,
            [{server_name, "rest"},
               {bind_address, "localhost"},
               {directory_index, ["index.html"]},
               {directory, {"/",
                     [{auth_type, mnesia},
                     {auth_name, "ocs.sigscale.org"},
                     {require_group, ["all"]}]}},
               {modules, [mod_alias, mod_auth,
                     mod_ocs_rest_dispatcher, mod_ocs_rest_accepted_content,
                     mod_ocs_rest_get, mod_ocs_rest_post, mod_ocs_rest_patch,
                     mod_ocs_rest_delete]},
               {port, 8888},
               {socket_type, {essl,
                     [{certfile, "%CERT_DIR%/cert.pem"},
                     {keyfile, "%CERT_DIR%/key.pem"}]}},
               {server_root, "%SERVER_ROOT%"},
               {document_root, "%APP_PRIV%/www"}]}]}]}].
