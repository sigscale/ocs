{application, %APP_NAME%,
	[{description, "Change this template description of the application!"},
		{vsn, "%APP_VERSION%"},
		{modules, [ocs,
				ocs_app,
				ocs_sup,
				ocs_fsm_sup,
				ocs_server,
				ocs_fsm,
				ocs_event_sup,
				ocs_event,
				ocs_event_server]},
		{registered, [ocs_fsm_sup, ocs_event]},
		{applications, [kernel, stdlib]},
		{env, [{foo, "set defaults of all environment variables!"}]},
		{mod, {ocs_app, []}}]}.
